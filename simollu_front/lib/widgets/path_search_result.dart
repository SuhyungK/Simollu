import 'package:flutter/material.dart';

class PathSearchResult extends StatelessWidget {
  const PathSearchResult({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
